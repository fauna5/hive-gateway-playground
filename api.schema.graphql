type Message {
  text: String!
  isCurrent: Boolean @deprecated(reason: "Don't use anymore") 
}

type UpdateMessageMutation {
  message(input: MessageUpdateInput!, old: String @deprecated(reason: "Don't use anymore")): Message!
}

input MessageUpdateInput {
  text: String!
  isCurrent: Boolean @deprecated(reason: "Don't use anymore")
}

type Query {
  message: Message!
}

type Mutation {
  updateMessage: UpdateMessageMutation!
}